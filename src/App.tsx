import './App.css';

function App() {
	return (
		<>
			<div className="w-[100vw] max-w-[100%] text-white text-[14px] select-none">
				{/* {isLoggedIn ? (
					<StatsProvider>
						<MainRoutes />
					</StatsProvider>
				) : (
					<MainRoutes />
				)} */}
			</div>
		</>
	);
}

export default App;
